# Hephaestus — runtime dependencies
# Python 3.10+

# Core PDF engine (render, rebuild, watermark, thumbnails, image extraction)
pymupdf>=1.24.0

# Encryption / decryption / permissions
pypdf>=4.0.0

# Enables AES-256 encryption in the Protect tool (falls back to RC4-128
# automatically when absent)
cryptography>=41.0.0

# Image pipelines (PDF <-> JPG/PNG, signature rasterization)
pillow>=10.0.0

# OCR bridge (the Tesseract *binary* must also be installed; see README)
pytesseract>=0.3.10

# OPTIONAL: native OS file drag & drop into the window.
# Without it, Hephaestus falls back to click-to-browse and Ctrl+V paste.
#   pip install tkinterdnd2
# tkinterdnd2>=0.9.2

# Build-only (not needed to run):
#   pip install pyinstaller
